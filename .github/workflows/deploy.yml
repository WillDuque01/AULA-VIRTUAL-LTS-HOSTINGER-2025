name: Deploy
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      HOSTINGER_APP_URL: ${{ secrets.HOSTINGER_APP_URL }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      HOSTINGER_SFTP_SERVER: ${{ secrets.HOSTINGER_SFTP_SERVER }}
      HOSTINGER_SFTP_PORT: ${{ secrets.HOSTINGER_SFTP_PORT }}
      HOSTINGER_SFTP_USERNAME: ${{ secrets.HOSTINGER_SFTP_USERNAME }}
      HOSTINGER_SFTP_PASSWORD: ${{ secrets.HOSTINGER_SFTP_PASSWORD }}
      HOSTINGER_PATH: ${{ secrets.HOSTINGER_PATH }}
      HOSTINGER_SSH_HOST: ${{ secrets.HOSTINGER_SSH_HOST }}
      HOSTINGER_SSH_PORT: ${{ secrets.HOSTINGER_SSH_PORT }}
      HOSTINGER_SSH_USER: ${{ secrets.HOSTINGER_SSH_USER }}
      HOSTINGER_SSH_PASSWORD: ${{ secrets.HOSTINGER_SSH_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
      - name: Build assets
        run: |
          npm ci
          npm run build
      - name: Upload via SFTP
        if: ${{ env.HOSTINGER_SFTP_SERVER != '' }}
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ env.HOSTINGER_SFTP_SERVER }}
          port: ${{ env.HOSTINGER_SFTP_PORT }}
          username: ${{ env.HOSTINGER_SFTP_USERNAME }}
          password: ${{ env.HOSTINGER_SFTP_PASSWORD }}
          protocol: sftp
          local-dir: ./
          server-dir: ${{ env.HOSTINGER_PATH }}
          exclude: |
            **/.git*
            **/.github/**
            node_modules/**
            vendor/**
            storage/framework/cache/**
            storage/logs/**
            tests/**
      - name: SSH post-deploy
        if: ${{ env.HOSTINGER_SSH_HOST != '' }}
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ env.HOSTINGER_SSH_HOST }}
          port: ${{ env.HOSTINGER_SSH_PORT }}
          username: ${{ env.HOSTINGER_SSH_USER }}
          password: ${{ env.HOSTINGER_SSH_PASSWORD }}
          script: |
            cd ${{ env.HOSTINGER_PATH }}
            if [ ! -f composer.phar ]; then curl -sS https://getcomposer.org/installer | php; fi
            php composer.phar install --no-dev --prefer-dist --no-progress --no-interaction
            php artisan config:cache
            php artisan migrate --force
            php artisan route:cache
            php artisan view:cache
            php artisan optimize
            php artisan storage:link || true
      - name: Smoke tests
        if: ${{ env.HOSTINGER_APP_URL != '' }}
        run: |
          curl --fail --show-error --silent "${{ env.HOSTINGER_APP_URL }}/es" >/dev/null
          curl --fail --show-error --silent "${{ env.HOSTINGER_APP_URL }}/es/dashboard" >/dev/null

      - name: Notify Slack
        if: ${{ always() && env.SLACK_WEBHOOK_URL != '' }}
        env:
          SLACK_WEBHOOK_URL: ${{ env.SLACK_WEBHOOK_URL }}
          STATUS: ${{ job.status }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -euo pipefail
          status_icon="✅"
          status_text="Deploy completado"
          if [ "$STATUS" != "success" ]; then
            status_icon="❌"
            status_text="Deploy con fallas"
          fi

          payload=$(jq -n \
            --arg text "$status_icon $status_text — $RUN_URL" \
            '{text: $text}')

          curl --fail --silent --show-error \
            -H "Content-Type: application/json" \
            -d "$payload" \
            "$SLACK_WEBHOOK_URL"

